#include <stdio.h>

int main(void) {
    int a, b, c, x, y, n1, n2, n3, B, h, vf, vi, a, t;
    float ex_a, ex_b, ex_c, ex_d, ex_e;
    /*Não assimilei nenhuma variável aos valores, pois o exercicio pedia apenas para escrever as expressões*/
    /*Por isso dá erro ao compilar*/

    ex_a = a + (b * c);
    ex_b = (a + b) * c;
    ex_c = (x*x) + (y*y);
    ex_d = (n1 + n2 + n3)/3;
    ex_e = ((b + B)*h)/2;
    vf = vi + (a*t); /*ex_f*/

    return 0;
}